kind = "web"
previewPath = "/"
title = "SPORTSBEEP — $BEEP Token"
version = "1.0.0"
id = "artifacts/sportsbeep"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20942

[services.development]
run = "pnpm --filter @workspace/sportsbeep run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sportsbeep", "run", "build" ]
publicDir = "artifacts/sportsbeep/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20942"
BASE_PATH = "/"
